import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // The Next.js dev-tools bubble is useful for framework debugging, but not for product UI work.
  devIndicators: false,
};

export default nextConfig;
