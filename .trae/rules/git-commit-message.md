---
alwaysApply: true
scene: git_message
---

在此处编写规则，自定义 AI 生成提交信息的风。


## 注意
- 提交描述必须用中文进行描述。

## 提交信息格式

提交信息的格式为：

```
<type>(<scope>): <subject>
```

其中，`type` 是提交的类型，`scope` 是影响的范围，`subject` 是提交的主体。
