# Lexloop 设计规范

> 适用范围：Lexloop 的网页端界面、组件、状态页和后续新增功能。  
> 目标：让产品始终像同一本「手绘学习笔记」——清晰、有节奏、略带笔触，而不是不同页面拼在一起的组件库。

## 1. 设计方向

### 核心隐喻

Lexloop 记录的是单词反复出现、逐渐被记住的过程。视觉应表现为：

- 一张带轻微纸张纹理的学习笔记；
- 深蓝色墨线勾勒结构与信息层级；
- 蓝、黄、橙三种标记色为进度和状态留下「手写标注」；
- 用不完全规则的圆角、虚线与排线填充制造温度，但不能牺牲阅读效率。

### 不可违背的原则

1. **内容先于装饰。** 任何纹理、阴影、旋转和动效都不能降低文字、表单和操作的可读性。
2. **墨线统一。** 一个界面只使用一套深蓝结构线；不要混入黑色描边、渐变玻璃或新的卡片风格。
3. **色彩有语义。** 蓝色用于系统、学习状态和所有结构墨线；黄色用于数量与重点标记；橙色用于提醒、新内容和当前内容上下文。
4. **可控的不规则。** 同一行中的控件必须严格对齐且使用统一圆角；仅独立卡片、下划线或纸条可有 1–2° 的轻微变化。
5. **真实可用。** 所有交互控件保留清晰的默认、悬停、按下、禁用和焦点状态。

## 2. 视觉令牌

优先复用 CSS 变量。页面级变量定义在 `apps/web/app/globals.css` 的 `.study-app` / `.welcome-page` 中。

| 用途 | 令牌 / 色值 | 使用说明 |
| --- | --- | --- |
| 主墨线、主文字 | `--border-color: #082877` | 结构边框、标题、正文、图标轮廓 |
| 纸张底色 | `--soft: #fffefa` | 页面、浅色容器与空白区 |
| 学习蓝 | `--teal: #315eb6` | 选中导航、主按钮、主动状态 |
| 标记黄 | `--gold: #ffdf4d` | 数量徽标、重点下划线与少量标记 |
| 提醒橙 | `--coral: #ff8351` | 新内容、提醒图标与当前内容标签 |
| 浅蓝底 | `#eaf2ff` | 复习类分区与低强调信息 |
| 浅黄底 | `#fff4b9` | 新词类分区与低强调信息 |

### 排线与纹理

- 页面纹理仅使用低对比度点阵：`radial-gradient(rgba(8,40,119,.06) .65px, transparent .8px)`，间距 `8px`。
- 数据柱、轻量统计卡可使用 45° 浅色排线；不要给文本区、输入框和大面积背景添加排线。
- 纸张纹理透明度应保持在可忽略级别，截屏和低分辨率屏幕上不应造成噪点感。

## 3. 字体与文案层级

| 场景 | 字体 | 规则 |
| --- | --- | --- |
| 中文展示标题、品牌 | `Noto Serif SC` | 用于首页主标题、页面标题、认证页标题 |
| UI、正文、表单 | `DM Sans`, `PingFang SC` | 用于仪表盘、菜单、表单与正文 |
| 数据、英文小标签 | `DM Mono` | 仅用于数值、日期、英文眉题与短注释 |

- 页面标题：`32–48px`，可使用黄色短下划线。
- 分区标题：`20–24px`，粗体，不使用渐变文字。
- 正文：`14–16px`；说明文字使用主墨线的 65–75% 对比度。
- 一个视觉区域内最多一个展示标题和一个辅助单行标签，避免把所有文字都加粗。

## 4. 布局与间距

### 全局栅格

- Desktop 主内容最大宽度：`1220px`；左右留白至少 `32px`。
- Dashboard 正文：内容区使用 `36px` 顶部间距；卡片组优先 `20–24px` 间距。
- 双列学习区：两列等宽，使用 `24–48px` 间距；小于 `680px` 时切为单列。
- 不用固定高度去截断正文。卡片内容增加时应自然撑高。

### 栏目头规范（重要）

所有列表/栏目头都采用三段布局，而不是依赖 `space-between` 让内容漂浮：

```
┌─────────────────────────────────────────────────────┐
│ [图标 + 标题 + 数量]       [状态/说明]    [操作按钮] │
└─────────────────────────────────────────────────────┘
```

1. **标题组**：图标、标题、数量徽标；始终靠左，内部间距 `10–12px`。
2. **状态/说明组**：可选，显示「待处理数量」「默认设置」等简短信息；没有说明时保留为空，不用装饰文字凑满。
3. **操作组**：始终靠右；按钮高度和栏目头一致的视觉节奏，按钮之间 `8–12px`。
4. **对称性**：同级双列的栏目头高度、底线位置、左右内边距必须一致；两列操作数量不同是允许的，但标题组的起点和底线必须一致。
5. **无操作栏目**：没有操作按钮时，不能为了“对齐”添加无意义按钮。保留右侧空区即可，但它必须是三段栅格中的操作槽位，而不是被 `justify-content: space-between` 拉开的偶然空白。

### 当前「待复习 / 新单词」问题

截图中的底线与顶部是对齐的，**不是几何错位**；问题在于视觉关系不清楚：

- `新单词` 的「+ 3」和设置在最右侧，而左侧 `待复习` 没有定义同样的操作槽位；
- 当前实现用 `justify-content: space-between`，标题区和按钮区被容器宽度强行拉开，空白没有表达语义；
- 两个栏目虽然都能成立，但作为一组时像两个不同的组件。

后续调整应把 `.word-column-head` 改为显式三段 Grid（例如 `grid-template-columns: 1fr auto auto`），并给两列使用相同的 head 结构。左列的操作槽可为空；右列填入「+ 3」和设置。不要通过增加假按钮解决该问题。

## 5. 组件规范

### 边框、圆角与阴影

- 默认结构边框：`2–3px solid var(--border-color)`。
- 独立卡片圆角：`7–13px`，同一组件内不混用直角与胶囊圆角。
- 分段控件、标签栏和同一行并列按钮：统一圆角（推荐 `8–12px`）；不得为每个角设置不同的半径。
- 浮起交互阴影：`2–4px 2–4px 0 var(--border-color)`；悬停时元素向左上移动 `2px`，阴影相应增加。
- 不使用柔和大面积投影、玻璃模糊或紫色渐变。

### 按钮

- 主按钮：深蓝底、纸张色文字、黄色投影。
- 次按钮：纸张底、深蓝描边与深蓝文字。
- 当前内容标签：暖橙底、纸张色文字、深蓝描边与深蓝硬阴影；不可引入额外的棕色或黑色结构线。
- 数量不是按钮：使用黄色底、深蓝描边的小徽标；最小高度 `32px`。
- 图标按钮：正方形，视觉尺寸 `40–44px`，必须有 `aria-label` 或文本提示。

### 图标

- 线框/单色图标默认继承 `currentColor`；标题图标在提醒或新内容情境下用橙色。
- 同一行图标保持同一视觉尺寸（通常 `18–22px`）。
- 自定义 SVG 使用 `viewBox`，不要用 `width`/`height` 属性固定缩放；尺寸交由 CSS 控制。

### 提示（Tooltip）

- 按钮和图标控件的悬停说明统一使用 `apps/web/components/tooltip.tsx` 的 `Tooltip` 组件；不要在 JSX 中直接新增 `data-tooltip`。图表数据点可保留其独立的数据说明样式。
- `Tooltip` 只包裹一个可交互子元素，并通过克隆子元素注入提示属性，不产生额外 DOM，避免破坏按钮组、Grid 或 Flex 布局。
- 图标按钮仍须保留准确的中文 `aria-label`；Tooltip 是鼠标悬停和键盘焦点的辅助说明，不能替代可访问名称。
- 默认显示在控件正上方并水平居中，使用纸张白 `#fffefa` 背景、深蓝 `2px` 描边和 `2px 2px 0 var(--border-color)` 硬阴影；不使用黄色背景或指向性小箭头。
- 提示文字保持单行，使用 `12px`、`800` 字重；只在 `:hover` 和 `:focus-visible` 时显示。空间不足时应调整控件位置或文案，不通过覆盖内容解决。

### 表单

- 输入框高度 `52–60px`，纸张底色、深蓝 `2px` 边框。
- 聚焦时只使用左上位移和硬阴影；不要改变页面布局。
- 错误使用橙色文字，放在相关字段或提交按钮上方。

## 6. 响应式与可访问性

- `920px` 以下：侧栏改为抽屉；工具栏可换行。
- `680px` 以下：双列内容改单列；关键操作仍可见，次要工具可进入横向滚动工具栏。
- 动效须遵守 `prefers-reduced-motion`。
- 主墨线文字与纸张底色保持高对比度；颜色不能是表达状态的唯一方式。
- 纯图标按钮、抽屉关闭、账户菜单等必须提供中文 `aria-label`。

## 7. 实现检查清单

提交任何 UI 改动前，确认：

- [ ] 使用了现有颜色令牌，没有引入无语义的新主题色。
- [ ] 页面、卡片和控件的边框颜色统一为主墨线。
- [ ] 同一层级的栏目头、按钮、数量徽标拥有一致的高度和对齐线。
- [ ] 空白承担了分组、阅读或操作槽位功能；不是 `space-between` 意外拉出的间隔。
- [ ] 主要断点（desktop / 920px / 680px）没有溢出、遮挡或不可达操作。
- [ ] 悬停、焦点、禁用状态和键盘可访问性仍然可用。
- [ ] 在真实数据文本变长时，布局不会截断或覆盖内容。

## 8. 禁止事项

- 禁止新页面回退到纯白卡片 + 灰色边框的通用 SaaS 视觉。
- 禁止每个卡片使用不同的阴影、圆角、颜色和动效语言。
- 禁止为了视觉平衡添加不能执行任务的按钮或图标。
- 禁止把所有区域都使用黄色、橙色或排线；强调必须稀缺。
- 禁止用绝对定位排列核心内容，除非是纯装饰元素。
